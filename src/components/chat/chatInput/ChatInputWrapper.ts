import styled from 'styled-components';

const ChatInputWrapper = styled.div`
  box-shadow: 0 -2px 0  0 rgb(0 0 0 / 0.05);
  width: 100%;
  display: flex;
  align-items: center;
  flex-grow: grow;
`;

export default ChatInputWrapper;